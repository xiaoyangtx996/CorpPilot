# CorpPilot 浏览器工作台需求与验收

权威范围：2026-09-06 用户下发的完整 Goalcraft 目标。完整浏览器应用与 Tauri 迁移规划；阶段原型不算整体完成。
交付分支 `codex/corppilot-agent-workbench`，每功能测试、审查、提交、立即推送及远端回读。已授权功能分支推送，不含 main、强推、PR、合并、发布及生产部署。

## 产品与技术评审

- PM 范围审查 Pass：会话驱动的个人团队工作台，继承角色/流程治理，普通任务不走全部管理层。
- Tech Lead/Ponytail 方向 Pass：单机 Python 控制服务、标准库 SQLite、有限 Docker Worker 池、静态 React/Vite 前端；不加入 Kubernetes、微服务或预制插件框架。
- UI 设计交接：[设计规格](workbench-design.md)。当前只是设计通过；运行界面和真实链路尚未验收。
- 长期身份、角色模板、会话、任务、Run、项目分开建模。工具镜像可复用，Worker 按任务回收，身份及批准记忆长期保留。
- 已盘点目录：13 个部门 SOUL、34 个专业岗位文件，共 47 个模板；最终还须核对实际配置引用，禁止照搬 README 岗位数。

## 验收矩阵

| ID | 要求 | 直接证据 | 当前状态 |
|---|---|---|---|
| R01 | 全部定义角色默认身份、稳定 ID、幂等启动、UI 新建编辑停用启用 | 来源清单、数据库重开、浏览器 CRUD、改名保留历史 | 身份存储/API/UI已测；配置来源与聊天历史关联仍待验证 |
| R02 | 秘书及任意身份私聊、董事会/项目群、成员/归档历史持久 | 浏览器建群收发、成员访问控制、服务重启 | 存储/API、重启与成员撤销竞态已测；浏览器/真实回复未完成 |
| R03 | 秘书目标形成任务，邀请必要成员，执行、评审并取得成果 | 完整浏览器与真实 CLI 交付录像/事件/可打开文件 | 任务契约/版本API和任务卡UI已测；执行API/UI、成果快照/下载及评审服务已测；F35/F36 Owner确认组建与恢复、F37提案API及F38浏览器提案/恢复/导入再次确认已验证；F38使用本地fixture，真实模型与CLI交付闭环未完成 |
| R04 | 按技能/依赖/资源/预算组队、有限并发、限流和排队 | 2 Worker 满额，第3排队，释放只启动一次；费用超限不执行 | 前置审批依赖调度、成果副本、依赖UI与并发队列已测；F37在Owner候选白名单及技能快照内生成建议，复用聊天队列；自动组队、资源/费用约束、双Worker未完成 |
| R05 | 两个实际 Docker Worker 工作区/HOME/配置/记忆隔离 | 容器挂载与读写探针，一失败另一完成，无宿主整盘/socket | 环境未就绪 |
| R06 | CLI/模型失败、超时、缺工具、取消不误成功 | 故障注入及真实退出码/实例终止证据 | Windows进程树退出/超时/取消、F40 Docker生命周期故障替身、F43/44批量依赖和停止闭环已测；F45模型unknown核查及范围门禁定向29项通过，全量570项及8子用例通过；真实CLI/模型任务及双Docker未验收 |
| R07 | Run ID、attempt、需求版本；重复/迟到回调不推进 | 同角色多任务、版本冲突和幂等回归 | 执行记录/版本/attempt/重复回调回归已测；CLI适配已接内部调度；真实模型与用户执行入口未验收 |
| R08 | 刷新/重启/容器重建恢复，副作用先回读再重试 | 重启真实服务/Worker、检查点、新attempt、日志回放 | F33/34执行核查、F40 Docker恢复核查替身、F42/44同键丢响应与服务重启401恢复已测；F45原模型Run不变、声明放行已授权queued而不重跑unknown；F46三类型模型核查/新key及重启丢响应浏览器路径已测，storage/release503三项独立QA通过且零POST；真实孤儿/容器恢复及检查点续跑未验收 |
| R09 | 私有/项目记忆权限、授权快照、单写者 | A标记B读不到，明确共享仅可见指定条目 | F30个人/群范围、批准版本与执行冻结快照、并发写入已测，F31浏览器入口已测；应用层上下文选择不等于真实OS/Docker隔离 |
| R10 | 经验候选来源/范围/审批/版本/回滚 | 产物与验收关联、批准前不生效、回滚还原 | F30/31手工候选审批/版本/回滚已测；F41/42批准成果一次模型复盘生成待审批候选、证据引用、隔离未选内容及浏览器审批恢复已通过本地模型fixture；真实供应商经验质量及Skill发布未验收 |
| R11 | Owner各身份目标/上下文摘要/活动/阻塞/成果/费用及干预 | 浏览器视角切换/修改/停止/恢复，观察不扩权 | 未完成 |
| R12 | 中文界面、真实配置、空态/校验/加载/错误/断线/键盘 | 真实浏览器E2E、原生参考尺寸与窄屏检查 | 身份界面与390px窄屏已测；完整聊天/任务界面未完成 |
| R13 | 旧测试、新后端/契约/E2E/容器/模型测试及独立QA | 分项命令、退出码、截图/事件与未验证说明 | F43完整551项及8子用例通过，随后新增容量2项另行通过；F44构建46模块及真实浏览器批量依赖/恢复闭环通过（受控runner）；F45定向29项通过，全量570项及8子用例通过（97.96s，session88107退出0），独立QA15项及审查Pass；F46构建47模块与三类型本地HTTP fixture浏览器链路已测，附加三项QA通过，零POST/非预期console或page错误；真实供应商/CLI/双Docker未验收 |
| R14 | 每功能提交、推送、远端回读和台账 | 本地/远端commit与验收映射 | 本地按功能提交，GitHub403阻塞 |
| R15 | Tauri迁移和本地运行/配置/备份/恢复文档 | 静态构建、API/sidecar生命周期、数据/权限/凭据/升级策略 | F32迁移规划与独立文档审查已完成，包含运行/配置、备份及升级回退边界；桌面改造和实际构建/安装/恢复未验收 |

## 阶段顺序与责任

可靠性 → 身份/持久状态 → 聊天/任务 → 双 Worker/恢复 → 组队/记忆 → 完整浏览器联调回归 → Tauri 文档/最终验收。
每阶段给出 Pass/Return/Escalate；PM 跟进全过程，前后端专业分工，QA 独立验证，主代理集成验收，DevOps 负责环境和恢复，文档随真实状态更新。

先完成已授权且无环境依赖的工作；关键真实链路未测不得判定目标完成。Mock只证明注入场景，不替代浏览器、Docker或真实模型。Docker/WSL系统变更若需要管理员或重启则单独请求最小授权；模型仅用现有授权配置和费用边界。

## 数据与交付边界

- SQLite 默认独立用户数据目录，支持 `CORPPILOT_WORKBENCH_HOME`；测试仅使用临时数据库。
- 旧 JSON 迁移前备份并校验数量/引用，切换后不长期双写；不得删除真实历史/记忆来通过测试。
- Worker 只能得到本次授权上下文、工具和挂载；Owner管理接口不能直接暴露给 Worker。
- 本轮不要求 Tauri 安装包、第二种 CLI、多机集群或自动训练模型；本轮必须真实完成至少一种 CLI 与双 Docker Worker。
- 分功能证据见 [交付台账](feature-delivery-log.md)，不将本地通过标为远端交付。

## F30 记忆能力边界

个人和群共享记忆服务已接入持久版本、Owner 候选审批与回滚、CLI 启动版本冻结。只有本身份和当前群的批准版本进入执行；查看其他身份不会改变执行上下文。F30 提供 HTTP API，F31 已补齐浏览器管理入口；任务自动复盘候选和 Skill 发布仍待实现，不能把这些增量标为完整记忆产品或真实 OS 隔离验收。

## F31 记忆管理界面

个人和群记忆已可在浏览器查看、选择成果来源提出候选、审批及回滚；刷新和版本冲突保留已发送原请求。该入口补齐 F30 的浏览器管理缺口。自动复盘候选、Skill 发布、真实隔离及整体验收仍未完成。

## F32 桌面衔接

本轮所需迁移契约见 [Tauri 2 迁移方案](tauri-migration.md)，已明确复用边界及后续验收。该文档完成不代表浏览器完整目标或桌面安装验收完成；不新增本轮安装包要求。

## F33 恢复边界

未知执行新增 Owner 人工核查 API，保留 unknown 与原始结果，通过独立不可变核查记录解除排队阻塞。核查必须针对原执行确认停止和外部影响；仍由控制器持有的执行不可核查。F34 已接入浏览器入口；自动孤儿识别和检查点续跑仍待实现，不能据此宣布完整恢复能力完成。

## F34 浏览器核查已完成的范围

Owner 可从全局入口查看未核查执行、原 attempt/需求版本和任务身份，填写依据并分别确认进程停止及外部影响已核查。断线或刷新保留原请求并同键核对；读取失败不显示为空。另一条不可变声明已先保存时，必须成功读回并明确确认，不能覆盖或声称原请求成功。任务窗口只对未核查的 unknown 阻断，历史版本、归档或身份停用不妨碍核对原执行。

构建、独立浏览器交互、故障注入与服务重启回读证据见 [验收报告 F34](acceptance-report.md#f34-浏览器未知执行核查验收)。样本中的原执行始终为 unknown，未改写退出码或自动重跑；人工声明不是机器停止证据，固定样本不代替真实 CLI、双 Docker 或自动恢复验收。

## F35 Owner 确认的项目协作计划（后端与 HTTP 已验证）

从私聊或群聊中选择一条本会话 Owner 目标消息，由 Owner 明确填写可共享摘要 `shared_brief`、项目标题、协调人及 1–16 项任务。每项任务包含负责人、目标、范围、验收标准和本计划内的前置任务；成员取协调人与任务负责人，协调人须属于源会话，所有选定身份须启用。依赖必须无环，不能引用计划外任务。

Owner 明确提交后，在一个事务中创建新项目会话、成员、共享摘要消息、v1 任务及依赖，并保存不可变创建回执；任一校验或写入失败均不保留半成品。相同源会话与请求键只接受同一计划，重试回读原创建结果。创建、重放和历史查询的回执均含 `approved_plan` 原计划，后续任务修改不会替换 Owner 当时确认的内容。新项目只接收明确共享的摘要和任务字段，不自动复制源私聊/群聊历史或个人记忆；源目标仅作为来源引用保留。

此增量不调用模型、不自动回复或启动执行。成员选择和任务拆分由 Owner 明确确认，尚不代表按技能/资源/预算智能组队；真实执行继续使用既有任务执行与评审门。验收覆盖原子回滚、DAG/成员校验、同键并发重试、历史不复制和零执行副作用。后端及 HTTP 定向 30 项通过、Tech Lead 审查 Pass；主代理全量 421 项及 8 子用例通过（65.34s）。F36 已补齐浏览器入口，F37/F38 已提供提案后端与浏览器导入再次确认；自动组队和真实执行闭环仍未完成，整体目标保持未完成。

## F36 浏览器项目组建与恢复（已验证）

Owner 消息下可打开“组建协作项目”，主动填写初始为空的共享摘要、项目名、协调人及任务计划；提交前展示全部任务、依赖和成员并要求明确确认。循环依赖阻止提交，来源和身份权限由后端复查；成功后显示当时批准的原计划并打开新项目。该入口由 Owner 决定拆分和成员，技能标签只作参考，不自动调用模型、组队或执行。

已发送原计划在当前标签页持久保存，切换会话或刷新后可从全局“协作计划与恢复”回到原来源核对。首次明确拒绝可保留全文重新编辑；核对原请求前清除早先拒绝标记，若后续响应未知则只沿原键恢复。读取失败不显示为空，源会话归档后仍能核对已提交原计划，已确认回执不因后续 GET 失败丢失。

主代理真实浏览器与独立数据回读验证普通创建、循环阻止、成员停用拒绝及重编辑、提交后丢失响应、GET 503、跨会话刷新恢复和同键只创建一次。两个样本项目共三任务，成员/依赖正确、无私聊历史复制，无 CLI/模型调用。最终 42 modules 构建通过（index-DS6DJ8Di.js），沿用 F35 后端全量 421 项及 8 子用例；本增量不重复宣称后端全量测试，也不代表模型提案、技能路由或完整目标完成。

## F37 白名单内的模型协作提案（后端与 API 已验证）

Owner 针对一条本会话目标消息，指定协调人及 1–100 个不重复、已启用的候选身份，显式请求模型提出任务与分工建议。模型输入仅包含该目标消息、协调人指令，以及候选公开的 id、name、template_id、skills 创建时快照；不附带源会话其他历史或个人记忆。协调人无需属于候选名单，但若要负责子任务也必须在名单中。模型结果须符合严格 JSON 结构、1–16 项任务和无环依赖，负责人只能来自白名单；非法结果记为失败，不作为可接受计划。

提案使用现有聊天 Runs 队列与并发/RPM限制，最多100个活动请求；Run ID 即提案 ID。配置未就绪时保持排队，配置就绪后才处理这次已明确提交的请求；排队可取消，失败或重启后未知不自动重试。执行前复查全部候选仍启用，完成前复查实际选中成员、协调人及源会话权限；编辑候选信息不改写已冻结资料，后续正式建项目仍由 F35 检查当前身份。

完成只保存提案及 Run 状态，不发送普通聊天答复、不建群或任务、不启动 CLI。提案不是 Owner 批准计划；须导入 F36，再由 Owner 审阅共享摘要、成员、任务和依赖并明确提交。F37提供API，F38已验证对应浏览器入口与导入再次确认，真实供应商链路仍未验收。后端全量与追加定向测试证据见 [F37 验收记录](acceptance-report.md#f37-模型协作提案后端验收)，整体目标保持未完成。

## F38 模型提案浏览器入口与导入（本地fixture链路已验证）

Owner可从目标消息或全局“模型协作提案”入口选择候选、明确确认调用模型，并查看同一提案的排队、运行及最终结果。原请求在发送前持久保存；刷新或切换会话后恢复原来源和内容，202接受后按已保存Run ID查询。首次明确拒绝可修改，重试前清除旧拒绝标记，后续响应未知时不得换键创建替代请求；已完成结果不因后续读取失败消失。候选后来停用时，已选项仍可取消选择，取消后不能重新选择停用项。

完成的模型提案须由Owner审阅，再显式导入F36。导入只形成新的可编辑草稿和创建请求，确认默认未勾选；Owner可修改共享摘要、负责人及任务，必须再次确认共享范围和完整计划后才创建项目。若F36已有待确认计划，导入被阻止并可转到原计划恢复入口。非法模型JSON显示失败、不提供可导入提案；配置禁用时排队可取消并在刷新后回读。

主代理完成类型检查、43模块最终构建（index-Dx-oq0Yc.js）和真实浏览器正常/故障流程，独立审查Pass。独立回读为4个Run（2完成、1失败、1取消）、3次实际本地HTTP模型调用、2个项目/4个v1任务、0次CLI，私聊前史未泄漏。桌面1315×1272及1280×720交互通过，未单独采集控制台日志或移动视口。此证据使用本地fixture，沿用F37后端测试，不代表真实供应商/CLI/Docker或整体目标完成。

QA服务重启后原取消结果及历史、调用次数和项目/任务数量不变，数据库完整性通过；测试服务、代理和Vite均已确认退出。网络受理未知可同键恢复，但服务端Run=unknown尚无人工核查/解除入口，只保留原请求且不允许替代调用；F33仅覆盖CLI核查。storage损坏/QuotaExceeded只有静态审查，未进行浏览器注入，不能将这些剩余恢复场景标为通过。

## F46 当前增量状态

三类型unknown人工核查、同key断线/重启401恢复以及核查后明确新key已完成本地HTTP fixture浏览器验证；原3unknown不变，新增reply/planning/retrospective各1次调用，复盘仅生成待审批候选且批准记忆v0不改。storage损坏与规划/复盘原pending release503边界独立QA三项Pass、零POST，桌面及390px移动截图已核查；F46本地fixture验收通过，本地提交已完成，立即推送403，远端未确认。真实供应商、CLI、双Docker和Tauri安装使用仍未验收，不标记全目标完成。

最终补充：主代理模型核查后端/控制器/API定向 **17 passed（4.39s）**，不记作新全量。正常7892服务原session69967先确认仍live再Ctrl-C退出1，保留browser-state由session19278重启；health200并回读index-DdLyA6xU.js，正常库仍只有历史unknown模型Run1、任务执行0、批次0，未作fixture写入。最终测试fixture session74866先poll确认live再Ctrl-C退出1，已停止；主代理重新运行f46_verify并落盘最终证据。真实供应商/CLI/Docker及整体目标未验收，F46 已本地提交，立即推送遭 GitHub403，远端回读为空；详见下方实际交付记录。

F46 实际交付记录：已直接读取外部 `H:\item\CorpPilot-test-evidence-20260906\f46-delivery-result.json`，提交 `18b5c481bd2cbb5d07cd21e953bf04f337673bb9` 成功后立即推送退出1，GitHub403：suiyue1990 无权写入 xiaoyangtx996/CorpPilot；远端回读退出0、`remote_head=null`。该记录报告提交后工作区干净，不能据此称远端交付完成。主代理已通过 git show 核实该提交为10文件、195行新增/20行删除。
## F47 同群 Agent 评议后端 / F48 浏览器范围

Owner在董事群或项目群选择A已完成Run发布的一条消息，明确授权不同的同群启用成员B，仅基于这条消息作一次评议。授权不默认包含源Owner目标、其他历史或A记忆；B自己的模板指令随授权冻结。完成后在原群发布B回复，保留来源Run与不可变请求回执。不会自动继续讨论、建群、派任务或启动CLI；Owner可再次明确授权下一次评议。

F47交付独立创建/列表/详情API，复用Run状态、排队取消和模型unknown人工核查。普通Owner回复规则不放宽；首次创建、执行前和发布前均检查同群、启用、不同身份及真实completed来源关系。相同请求键只回原结果，跨类型或改内容必须拒绝；未核查unknown不能以新键替代。模型结果或核查声明不等于Owner批准任务或记忆。

F47后端实施、独立QA/TechLead/Ponytail及API验收Pass；完整596项及8子用例通过（105.83s，session37270退出0），包含目标角色冻结和HTTP严格字段校验。A→B各一次本地HTTP模型调用、并发幂等及unknown核查后明确新key已测。F48消息选择、确认、持久化和恢复入口已通过本地fixture浏览器验收，详见F48验收记录。本阶段不代表真实供应商质量、CLI、双Docker或整体目标完成。

F47 实际交付：直接核实外部 `H:\item\CorpPilot-test-evidence-20260906\f47-delivery-result.json`，本地提交 `a4f7875c73bc29d0d1570b4e3aaaf9f17f2562c4` 成功，11文件、461行新增/19行删除；随即推送退出1，GitHub403（suiyue1990无目标仓库写权限）。远端回读退出0但 `remote_head=null`，因此远端交付仍阻塞；F48五个前端文件未包含在F47提交中。

## F48 群内逐次授权评议界面（本地fixture已验证）

董事群/项目群Agent消息下提供“请另一成员评议”，展示源作者及选定消息全文，只能选择同群启用且不同于源作者的成员。Owner勾选确认后才提交一次模型调用；明确仅分享此条，不带其他历史或原Owner目标，可能产生费用，不自动续轮。评议完成后提示到原群刷新查看；后续评议仍须重新选取并确认。

全局“Agent评议与恢复”入口无需预选会话即可恢复原请求；已有pending优先回到原来源，历史按所选群隔离。发送前冻结完整payload和源正文；首次明确4xx可修改，401及响应丢失保留原key，重试先清旧拒绝。202仅保存编号，独立GET精确核验后才确认结果并允许释放；unknown须通过人工核查，再GET原Run声明成功方可释放，失败撤销旧核查许可且保留pending。

类型检查及48模块构建通过（index-BDbMUDFL.js）；群内明确逐次授权、断线与401同键恢复、unknown核查和排队取消已通过本地fixture浏览器验收，PM/QA/TechLead/Ponytail增量Pass。证据见[验收记录](acceptance-report.md#f48-群内评议界面验收本地fixture验收pass)。F48已本地提交，立即推送403阻塞，远端未确认；实际记录见下文。真实供应商、CLI、双Docker和整体目标仍未验收。


F48实际交付：已直接核实外部 `H:\item\CorpPilot-test-evidence-20260906\f48-delivery-result.json`，本地提交 `43be70154bc7d373932a0cd5f14ce672fbfef48f` 成功，9文件、179行新增/13行删除；立即推送退出128，GitHub返回403（suiyue1990无写权限），远端回读退出0但remote_head=null。记录中working_tree_status为空，提交后工作树干净；本地验收与提交不等于远端交付成功。


## F49 批准完整计划并创建启动（后端增量）

Owner明确确认完整共享摘要、成员、任务范围、验收及依赖后，一次操作原子创建项目和全部任务的首轮执行批次，省去建群后的再次入队确认。失败不留下半个项目或部分执行；仅创建项目的旧授权不能通过重用请求键扩大成执行授权。任务不会自动获得execute权限。

F49保留既有依赖成果Owner审批、unknown阻断和固定实例停止；入队不代表已经运行或交付。自动规划、同授权中间成果交接、货币预算硬门仍未完成；当前CLI超时/并发不能充当费用上限。F50复用现有计划预览增加界面操作，尚未浏览器验收。F49验证记录见acceptance-report.md对应段。
